[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# Ensure SPA routing works (React Router)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200


